package com.maxima.userService.config;

/**
 * Класс {@code ApiConfig} содержит конфигурацию API, включая пути (endpoints) для работы с
 * пользователями.
 * <p>
 * Этот класс предназначен для хранения статических констант, которые используются в качестве
 * базовых URL-путей для API. Это позволяет централизованно управлять путями и избежать дублирования
 * строк в коде.
 * </p>
 */
public class ApiConfig {

  public static final String USERS = "/api/v1/users";

}
